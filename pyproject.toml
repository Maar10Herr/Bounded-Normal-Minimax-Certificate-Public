[project]
name = "bounded-normal-mean-certificate"
version = "0.1.0"
description = "Deterministic adaptive-support computation and independent certificates for the bounded normal mean problem"
requires-python = ">=3.11"
dependencies = [
    "numpy==2.3.2",
    "pillow==11.3.0",
    "python-flint==0.8.0",
]

[tool.uv]
package = false
