# Security policy

Verification requires no API keys, service credentials, database, or network
access. Report vulnerabilities privately through GitHub's security-advisory
interface. Include the affected release, command, platform, and a minimal
reproducer.

Certificate acceptance, interval-arithmetic, or manifest-integrity failures are
security-relevant because they can invalidate a computer-assisted proof.
